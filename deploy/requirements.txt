# Runtime container dependencies (no FastAPI/uvicorn - BedrockAgentCoreApp
# provides the /invocations + /ping server itself).
strands-agents>=1.0.0
strands-agents-tools>=0.1.0
bedrock-agentcore>=0.1.0
boto3>=1.40.0
pyyaml>=6.0
python-dotenv>=1.0.0
# Observability is auto-instrumented by AgentCore Runtime, but including the
# ADOT distro makes traces work in non-runtime hosts too.
aws-opentelemetry-distro>=0.10.0
